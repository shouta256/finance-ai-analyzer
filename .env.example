###############################
# Safepocket – Backend env (example)
# Copy to .env and fill values for local/dev.
# NOTE: .env is gitignored. Do not commit real secrets.
###############################

# Enable Cognito-backed auth instead of dev JWT
SAFEPOCKET_USE_COGNITO=true

# Amazon Cognito Hosted UI domain (no trailing slash)
# Example for native app client:
COGNITO_DOMAIN=https://us-east-1mfd4o5tgy.auth.us-east-1.amazoncognito.com

# Cognito App Client IDs
# Web client id (Hosted UI for web)
COGNITO_CLIENT_ID_WEB=
# Native client id (custom scheme; typically no secret)
COGNITO_CLIENT_ID_NATIVE=p4tu620p2eriv24tb1897d49s
# (Optional) Fallback/legacy single client id
#COGNITO_CLIENT_ID=

# Redirect URI registered in Cognito for the native app
# Use the app’s custom scheme.
COGNITO_REDIRECT_URI=safepocket://auth/callback

# Optional overrides (only if defaults don’t fit)
#COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_mFd4O5tGY
#COGNITO_AUDIENCE=${COGNITO_CLIENT_ID_WEB},${COGNITO_CLIENT_ID_NATIVE}
#COGNITO_CLIENT_SECRET=

# Backend dev JWT (ignored when Cognito enabled). Keep length >= 32 if set.
#SAFEPOCKET_DEV_JWT_SECRET=local-dev-shared-secret-change-me-32
