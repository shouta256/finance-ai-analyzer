# 開発モード用のダミーJWT署名キー（本番では絶対に使用しないでください）
# Backend(application.yml)のデフォルトと一致させる必要があります
SAFEPOCKET_DEV_JWT_SECRET=dev-secret-key-for-local-development-only
LEDGER_SERVICE_URL=http://localhost:8081
NEXT_PUBLIC_COGNITO_DOMAIN=us-east-1mfd4o5tgy.auth.us-east-1.amazoncognito.com
NEXT_PUBLIC_COGNITO_CLIENT_ID=5ge4c1b382ft2v71rvip0rrhqv
COGNITO_CLIENT_SECRET=1bgkn0kh7plfnmlo1hm3obqdk5es9tn4dlarvbnslnfsicgea544
# 任意 (省略時は window.origin + /auth/callback )
# 本番は https://app.shota256.me/api/auth/callback/cognito を Cognito Callback URLs に登録済み
NEXT_PUBLIC_COGNITO_REDIRECT_URI=http://localhost:3000/auth/callback
# App Client で許可されたスコープに合わせる (openid email phone)。profile は未許可なので除外
NEXT_PUBLIC_COGNITO_SCOPE=openid email phone
# 開発でデモログインを出したい場合のみ
NEXT_PUBLIC_ENABLE_DEV_LOGIN=true